import { type Tag } from '../schema';

export async function getAllTags(): Promise<Tag[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all unique tags from all notes
    // along with their usage count, ordered by usage frequency for better UX.
    // This helps users see popular tags and organize their notes effectively.
    return [];
}